#!/usr/bin/env bash

#! is called as She-Bang / Bang / Hash Bang

# it denotes the path of interpreter and also ensures that script will be executed using that interpreter

# She-Bang has to be in the very line
# Only one she-bang is possible

# Above lines are comments which will not be executed by the interpreter.

ls
cat /etc/passwd
